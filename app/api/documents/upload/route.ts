import {NextResponse} from 'next/server';export async function POST(req:Request){const fd=await req.formData();const file=fd.get('file');return NextResponse.json({mode:'mock',message:'Document accepted for simulated OCR/NLP extraction',fileName:file instanceof File?file.name:'unknown',extracted:{documentType:'Compensation Document',projectId:'NH-112-MAL-01',riskIndicators:['pending verification','signature review']}})}
