import {NextResponse} from 'next/server';import {findProject,recommendations} from '../../../../../lib/api';export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const id=(await params).id;if(!findProject(id))return NextResponse.json({error:'Project not found'},{status:404});return NextResponse.json({data:recommendations(id)})}
