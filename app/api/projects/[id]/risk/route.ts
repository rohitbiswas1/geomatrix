import {NextResponse} from 'next/server';import {findProject} from '../../../../../lib/api';import {calcRisk} from '../../../../../lib/data';export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const p=findProject((await params).id);if(!p)return NextResponse.json({error:'Project not found'},{status:404});return NextResponse.json({data:{...calcRisk(p),confidence:p.confidence}})}
