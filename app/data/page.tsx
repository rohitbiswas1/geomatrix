'use client';import {useState} from 'react';import {Upload,CheckCircle2} from 'lucide-react';export default function Data(){const [files,setFiles]=useState<string[]>([]);function add(e:React.ChangeEvent<HTMLInputElement>){if(e.target.files) setFiles([...files,...Array.from(e.target.files).map(f=>f.name)])}return <div className="page"><div className="head"><div><div className="eyebrow">Data operations</div><h1 className="h1">Data Management</h1><div className="sub">Validate project, parcel, compensation, legal and document datasets before prediction.</div></div></div><div className="grid two"><div className="panel"><div className="paneltitle">Upload data</div><div className="form" style={{marginTop:14}}><label className="btn" style={{textAlign:'center',padding:22,borderStyle:'dashed'}}><Upload size={18}/><br/>Upload CSV / XLSX / GeoJSON / PDF<input type="file" multiple accept=".csv,.xlsx,.geojson,.json,.pdf" style={{display:'none'}} onChange={add}/></label></div>{files.map(f=><div className="stage" key={f}><CheckCircle2 size={15}/><b>{f}</b><span className="muted">Uploaded</span></div>)}</div><div className="panel"><div className="paneltitle">Data quality</div><div className="grid three" style={{marginTop:14}}>{[['Records','12,482'],['Missing fields','184'],['Duplicates','27'],['Invalid coordinates','6'],['Validation','98.4%'],['Ready for prediction','12,265']].map(x=><div className="kpi" key={x[0]}><div className="label">{x[0]}</div><div className="value" style={{fontSize:18}}>{x[1]}</div></div>)}</div></div></div><div className="panel" style={{marginTop:14}}><div className="paneltitle">Processing pipeline</div><div className="grid" style={{gridTemplateColumns:'repeat(5,1fr)',marginTop:18}}>{['Uploaded','Validated','Cleaned','Feature Engineered','Ready for Prediction'].map((x,i)=><div className="panel" key={x} style={{textAlign:'center',background:i<4?'#f8fafc':'#eef8f3'}}><div style={{fontWeight:800,fontSize:12}}>{i+1}</div><div style={{fontSize:10,marginTop:5}}>{x}</div></div>)}</div></div><div className="panel" style={{marginTop:14}}><div className="paneltitle">Document / NLP intelligence</div><p className="sub">Prototype extraction interface for land records, compensation documents, legal notices, approval documents and rehabilitation documents.</p><div className="grid three"><div className="rec"><b>Mock OCR/NLP</b><p>Document type, reference number, project ID, parties, date, authority and risk indicators.</p></div><div className="rec"><b>Future architecture</b><p>OCR → NER → classification → legal-text processing.</p></div><div className="rec"><b>Transparency</b><p>No actual OCR is claimed in this prototype; extraction is simulated.</p></div></div></div></div>}
