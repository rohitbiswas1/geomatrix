'use client';import {useEffect,useRef,useState} from 'react';import Link from 'next/link';import mapboxgl from 'mapbox-gl';import {projects,riskLevel} from '../../lib/data';import {Layers,Search,SlidersHorizontal} from 'lucide-react';export default function MapPage(){const ref=useRef<HTMLDivElement>(null);const [ready,setReady]=useState(false);const [selected,setSelected]=useState<typeof projects[0]|null>(null);useEffect(()=>{const token=process.env.NEXT_PUBLIC_MAPBOX_TOKEN;if(!ref.current||!token)return;mapboxgl.accessToken=token;const map=new mapboxgl.Map({container:ref.current,style:'mapbox://styles/mapbox/streets-v12',center:[84.5,24.8],zoom:4.7});map.addControl(new mapboxgl.NavigationControl(),'top-right');map.addControl(new mapboxgl.FullscreenControl(),'top-right');projects.forEach(p=>{const el=document.createElement('div');el.style.width='15px';el.style.height='15px';el.style.borderRadius='50%';el.style.border='2px solid white';el.style.background=p.risk>=75?'#c43d3d':p.risk>=50?'#d8872c':p.risk>=25?'#d7a20c':'#16845b';el.style.boxShadow='0 1px 5px #0006';el.onclick=()=>setSelected(p);new mapboxgl.Marker(el).setLngLat([p.lng,p.lat]).addTo(map)});setReady(true);return()=>map.remove()},[]);return <div className="page"><div className="maplayout"><section className="mapbox"><div ref={ref} className="mapcanvas"/>{!process.env.NEXT_PUBLIC_MAPBOX_TOKEN&&<div className="mapfallback"><div><Layers size={40}/><h3>Map configuration required</h3><div className="sub">Set NEXT_PUBLIC_MAPBOX_TOKEN in .env.local to enable Mapbox GL JS.<br/>The rest of the prototype remains fully usable without the token.</div></div></div>}{selected&&<div style={{position:'absolute',left:18,bottom:18,width:290,background:'#fff',border:'1px solid var(--line)',padding:14,borderRadius:6,boxShadow:'0 8px 25px #0002'}}><div className="eyebrow">Project preview</div><h3 style={{fontSize:14,margin:'6px 0'}}>{selected.name}</h3><div className="sub">{selected.state} · {selected.district}</div><div style={{display:'flex',justifyContent:'space-between',margin:'12px 0'}}><span className={'risk '+riskLevel(selected.risk).toLowerCase()}>{riskLevel(selected.risk)} {selected.risk}/100</span><b>{selected.delay}% delay</b></div><div className="muted">Main driver: {selected.driver}</div><Link className="btn primary" style={{display:'inline-block',marginTop:12}} href={'/projects/'+selected.id}>Open Project</Link></div>}</section><aside className="mapside"><div className="eyebrow">GIS Risk Intelligence</div><h2 style={{fontSize:20,margin:'5px 0'}}>Spatial risk overview</h2><div className="search" style={{maxWidth:'none',margin:'14px 0'}}><Search size={14}/><input placeholder="Search map projects…"/></div><div className="mapmetric"><div><div className="muted">Total projects</div><b>128</b></div><div><div className="muted">Critical</div><b>12</b></div><div><div className="muted">High-risk</div><b>27</b></div><div><div className="muted">Affected area</div><b>18.4k ha</b></div></div><div className="panel" style={{padding:12}}><div className="paneltitle"><SlidersHorizontal size={13}/> Filters</div><div className="form" style={{marginTop:10}}><select><option>All States</option></select><select><option>All Districts</option></select><select><option>All Risk Levels</option></select><select><option>All Acquisition Stages</option></select><select><option>All Project Types</option></select></div></div><div style={{marginTop:18}}><div className="paneltitle">Layers</div><div className="legend"><div className="legendrow"><span className="legenddot" style={{background:'#c43d3d'}}/> Critical projects</div><div className="legendrow"><span className="legenddot" style={{background:'#d8872c'}}/> High-risk projects</div><div className="legendrow"><span className="legenddot" style={{background:'#d7a20c'}}/> Medium-risk projects</div><div className="legendrow"><span className="legenddot" style={{background:'#16845b'}}/> Low-risk projects</div></div></div><p className="footer-note">Risk Heatmap · Project Markers · Acquisition Areas · Infrastructure Layer · Street/Satellite toggle are architected for Mapbox layers; demo markers use synthetic GeoJSON-ready project data.</p></aside></div></div>}
