'use client';import Link from 'next/link';import {usePathname,useRouter} from 'next/navigation';import {Bell,BrainCircuit,Database,FileText,LayoutDashboard,Map,Search,Settings,ShieldCheck,Users,LogOut} from 'lucide-react';import {useState} from 'react';export function Shell({children}:{children:React.ReactNode}){const path=usePathname();const router=useRouter();const [q,setQ]=useState('');const login=path==='/login';if(login)return <>{children}</>;const nav=[['/dashboard','Command Center',LayoutDashboard],['/projects','Projects',Users],['/map','GIS Risk Map',Map],['/alerts','Alerts',Bell],['/analytics','Analytics',BrainCircuit],['/reports','Reports',FileText],['/data','Data Management',Database],['/settings','Settings',Settings]];return <div className="app"><aside className="sidebar"><div className="brand"><div className="brandmark"><Map size={19}/></div><div><b>GEOMATRIX</b><small>LAND ACQUISITION AI</small></div></div><nav className="nav">{nav.map(([href,label,Icon])=><Link key={href as string} href={href as string} className={path.startsWith(href as string)?'active':''}><Icon size={16}/><span>{label as string}</span></Link>)}</nav><div className="sidebottom"><div className="userline"><div><b style={{fontSize:11}}>Administrator</b><div className="role">Prototype account</div></div><ShieldCheck size={15}/></div><button className="btn" style={{marginTop:12,width:'100%'}} onClick={()=>router.push('/login')}><LogOut size={13}/> Logout</button></div></aside><main className="main"><header className="topbar"><div className="crumb">GovTech Decision Support / {path.split('/')[1]||'dashboard'}</div><div className="search"><Search size={14}/><input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&q&&router.push('/projects?search='+encodeURIComponent(q))} placeholder="Search project, district or project ID…"/></div><div className="topright"><div className="status"><i className="dot"/> System operational</div><div className="date">07 Sep 2026 · 20:45 IST</div><Bell size={16}/></div></header>{children}</main></div>}
